# Vibess

`/client` 
Frontend react app

in Local Development listen to port `3002` change in `.env` `VITE_PORT` and `VITE_HOST`

`/server`
WebSocket `socket.io` server listening to port `3001` change in `.env` or set `PORT` and `HOST`